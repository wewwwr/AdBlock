name: Update Blocklist

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

permissions:
  contents: write

# Эта настройка принудительно включает Node.js 24 и убирает предупреждение
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  update-list:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Run Python script
        run: python update_list.py

      - name: Check All Parsers
        run: python check_parser.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add my_custom_blocklist.list
          git add reports/parser_report.md
          git commit -m "Auto-update blocklist and parser report" || echo "No changes to commit"
          git push
